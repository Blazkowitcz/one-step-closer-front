import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'One Step Closer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
